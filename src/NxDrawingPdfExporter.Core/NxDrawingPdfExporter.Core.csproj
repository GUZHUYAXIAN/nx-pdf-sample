<Project Sdk="Microsoft.NET.Sdk">

  <!--
    NX-independent core rules: discovery, preflight, sheet selection facts,
    safe publication orchestration, job state machine.
    Must never reference NXOpen.
  -->

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <RootNamespace>NxDrawingPdfExporter.Core</RootNamespace>
    <AssemblyName>NxDrawingPdfExporter.Core</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\NxDrawingPdfExporter.Contracts\NxDrawingPdfExporter.Contracts.csproj" />
  </ItemGroup>

</Project>
