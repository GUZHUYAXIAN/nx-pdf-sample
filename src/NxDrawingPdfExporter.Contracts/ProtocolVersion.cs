namespace NxDrawingPdfExporter.Contracts
{
    /// <summary>Wire protocol version shared by GUI and Worker.</summary>
    public static class ProtocolVersion
    {
        public const string Current = "1";
    }
}
